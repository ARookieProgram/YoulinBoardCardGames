"""房间管理（**只读**监控存活房间）。

本应用**只有一个方向**：把玩家库 `db_scmj` 的 `t_rooms` 读出来给运营看。
它没有模型、没有迁移、没有任何写操作，因此也不需要在管理平台的库
（`db_scmj_admin`）里建表。

几条必须知道的边界：

* SQL 不在本应用里，而在 `apps/players/player_source.py`——那是管理平台读玩家库的
  **唯一通道**（`t_users` 与 `t_rooms` 都在同一个库里，多一条通道就多一处绕过
  只读校验的可能）。本应用只负责校验入参、拼出参与挂权限；
* `t_rooms` 里只有**尚未销毁**的房间：游戏服销毁房间时会删掉整行，
  进程重启时再用这些行把房间恢复回内存。所以这里看到的就是"当前存活房间"；
* **强制解散 / 踢人这类运维动作本期不实现**，只提供已预留的入口
  （`/api/rooms/<room_id>/dissolve/`，恒返回 `reserved: true`）。
  真要做需要平台反向调用游戏服的内部接口，是另一次跨进程改动。
"""
